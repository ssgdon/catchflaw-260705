# 캐치플로 UI 표준 (v1 · 2026-07)

모든 화면 수정은 이 문서와 `css/tokens.css`를 기준으로 한다. 여기 없는 색/크기/용어를 새로 만들지 않는다.

## 1. 용어 표준 (DB와 UI 통일)

DB(`reviews_analysis_new.grade`)가 기준이다. UI가 DB를 따른다.

| 개념 | 표준 용어 | 금지 (기존 혼용) |
|---|---|---|
| 리뷰 태그 3단계 | **심각 / 주의 / 정상** | ~~경고~~ (→주의) |
| 호텔 등급 배지 3단계 | **위험 / 주의 / 양호** | ~~보통~~ (→양호) |
| 종합 지표 | **실망 확률 N%** (실측치) | ~~점수~~ 라는 단어 금지 |
| 카테고리 (6개, 띄어쓰기 고정) | 위생 경보 · 오감 지옥 · 시설 사기단 · 동선 파괴자 · 불친절 레이더 · 안전 그림자 | ~~시설사기단~~ 등 붙여쓰기 |

## 2. 시맨틱 컬러 — 3색 외 사용 금지

같은 의미에 3벌의 색이 혼용되던 것(#fa5252/#e92424, #d06779, #e9a424/#F0A028…)을 아래로 통일.

| 의미 | 배경(진) | 텍스트(soft 위) | soft 배경 |
|---|---|---|---|
| 위험/심각 | `--danger` #FA5252 | `--danger-text` #E92424 | `--danger-soft` #FDE9E9 |
| 주의 | `--warning` #F0A028 | `--warning-text` #C9820A | `--warning-soft` #FDF6E9 |
| 양호/정상 | `--safe` #5EA5E7 | `--safe-text` #3D87CC | `--safe-soft` #E9F2FB |

- 진한 배지(썸네일 위) = 진한 배경 + 흰 글자. 연한 칩(카드 안) = soft 배경 + text 색.
- 브랜드 보라(`--primary`)는 강조·CTA·수치 하이라이트 전용. 등급 표현에 쓰지 않는다.

## 3. 타이포그래피

- 폰트: Pretendard 고정. 자간 전역 `-0.02em`. 숫자 정렬 필요한 곳 `font-variant-numeric: tabular-nums`.
- 스케일 (이 8단계 외 임의 px 금지):

| 토큰 | 크기 | 용도 | 굵기 |
|---|---|---|---|
| `--fs-display` | 30 | 히어로 타이틀, 실망확률 대형 숫자 | 800 |
| `--fs-h1` | 24 | 상세 호텔명 | 800 |
| `--fs-h2` | 22 | 섹션 타이틀 | 800 |
| `--fs-h3` | 18 | 서브 타이틀 | 800 |
| `--fs-body-lg` | 17 | 카드 호텔명, 강조 본문 | 700 |
| `--fs-body` | 15 | 본문, 리뷰 텍스트 | 500 |
| `--fs-caption` | 13 | 메타, 뱃지, 날짜 | 500~700 |
| `--fs-micro` | 12 | 각주, 태그 | 500 |

- 행간: 제목 1.35, 본문 1.5.

## 4. 간격·라운드

- 4px 그리드(`--sp-*`)만 사용. 페이지 좌우 `--page-x`(20px), 섹션 리듬 `--section-gap`(48px).
- 라운드: 카드 20 / 이미지 16 / 버튼·입력 14 / 뱃지·칩 8. 이 4단계 외 금지.

## 5. 등급 산정 기준 (배지 ↔ 점수체계 v2 연동)

- **실망 확률** = 최근 가중 기준 "심각 태그 리뷰 비율" 실측치 (후쿠오카 평균 12%, 분포 2.7~35.2%).
  - 배지 표기는 반올림 정수 (예: "실망 확률 32%"). 80% 같은 목업 값은 실데이터에서 나올 수 없음.
- **호텔 등급 배지**: 위험 = 도시 평균의 1.5배 이상(후쿠오카 ≥18%) · 양호 = 0.8배 이하(≤9.6%) · 그 사이 = 주의. *(초기값, 노출 분포 보고 캘리브레이션)*
- **카테고리 위험도(0~100)**: 도시 평균 = 50, 3배 = 100. 색 구간: 양호 <45 / 주의 45~70 / 위험 ≥70.
- **점수 미노출**: 분석 리뷰 30건 미만 → "리뷰 수집중" 배지, 등급·확률 미표기.
- 가드: 카테고리 불만 리뷰 5건 미만이면 위험도 상한 65.

## 6. MVP 스코프 규칙 (후쿠오카 검증판)

- **후쿠오카만 지원.** 검색 결과에 타도시 호텔을 섞지 않는다.
- 미지원 도시 검색 시: 검색 화면에서 즉시 "아직 후쿠오카만 지원해요" 안내 (상세 진입·알림받기 유도 금지).
- **로그인 없음**: 카카오 로그인, "로그인하고 리뷰 보기" 블라인드 제거. 모든 리뷰 공개.
- 제거 대상 UI: 분석요청 버튼, 분석 완료 시 알림 받기, 카카오 공유의 로그인 의존 기능.
- 유지: 찜/최근 본 호텔은 localStorage로 (로그인 없이).

## 7. 데이터 표기 규칙

- 별점 없는 리뷰(Trip.com 등 17,315건)는 별 대신 출처 아이콘+텍스트만 표기. 별 0개로 그리지 않는다.
- 리뷰 인용문은 `reviews_analysis_new.quote`(원문 발췌) 사용, 카테고리 키워드는 `--primary`로 하이라이트.
- 날짜는 `YYYY. MM. DD`. 도시 평균 비교 문구는 "후쿠오카 평균 대비 N배" 형식.

## 8. AI 맞춤 추천 (recommend.js)

고객에게 내부 카테고리 용어를 **절대 노출하지 않는다.** 아래 고객 언어 라벨을 정본으로 쓴다.

| 내부 카테고리 | 마법사 라벨 | 요약 칩(rec 헤더) | 보조 키워드 |
|---|---|---|---|
| 위생 경보 | 🧼 더러운 건 못 참아 | 🧼 청결 | 침구·벌레·곰팡이 |
| 오감 지옥 | 👂 시끄럽고 냄새나면 싫어 | 👂 조용 | 소음·악취 |
| 시설 사기단 | 🏚️ 낡은 시설은 실망이야 | 🏚️ 시설 | 노후·냉난방·와이파이 |
| 동선 파괴자 | 🗺️ 위치가 제일 중요해 | 🗺️ 위치 | 역까지 거리·접근성 |
| 불친절 레이더 | 💬 불친절은 못 넘어가 | 💬 응대 | 직원 응대 |
| 안전 그림자 | 🔒 안전이 최우선이야 | 🔒 안전 | 보안·치안 |

- **거짓 없는 숫자 원칙**: 추천 이유 문장에 "92% 일치" 같은 만들어낸 매칭 퍼센트를 쓰지 않는다. 실측치(위험도·백분위·실망 확률·가격)만 사용.
- **matchScore**는 정렬용 내부 값 — 화면에 점수로 노출하지 않는다.
- 마법사·순위 마커·CTA·뱃지 색은 전부 `--primary`/`--primary-deep`. 메달은 이모지(🥇🥈🥉) — 금/은/동 커스텀 색 금지.
- 지도 프로그래밍 이동은 `setView([lat,lng], 15, {animate:false})` 필수 (애니메이션 setView는 프로그래밍 호출 시 무시되는 Leaflet 이슈).
- 상세 설계·산식은 `AI-RECOMMEND-DESIGN.md` 참조.
