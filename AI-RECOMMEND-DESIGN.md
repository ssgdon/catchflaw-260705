# AI 맞춤 추천 기능 설계서 (+ 지역 지도 줌 수정)

v1 · 2026-07-05 · 설계: Fable / 구현: Opus
철학: **심플한 3스텝 · 고객의 언어 · 거짓 없는 숫자 · 기존 디자인 시스템 100% 재사용**

> ⚠️ 구현 전 필독: §6 디자인 가드레일. 새 색·새 폰트크기·새 컴포넌트 패턴을 만들지 말 것.
> 구현 난이도 참고: **필요한 데이터가 전부 `data/index.js`에 이미 있다** (cs=카테고리점수, cb=등급, krw, pb, lat/lng, p, g, rc). 백엔드·재생성 불필요, 프론트 JS만으로 완성된다.

---

## 0. 한 줄 요약

메인의 플로팅 버튼 "✨ AI 추천" → 바텀시트 3스텝(못 참는 것 1·2·3순위 → 예산 → 지역) → `search.html?rec=...`로 이동해 **맞춤 점수 순위 TOP 10을 지도(순위 마커) + 리스트(추천 이유 문장)** 로 보여준다.

---

## 1. 먼저: 지역 선택 시 지도 줌 수정 (기존 버그)

**현상**: 지역 칩(하카타역 등)을 눌러도 지도가 거의 안 변하는 느낌.
**원인**: `run() → drawMap(pool, true) → map.fitBounds(전체 필터 결과 좌표, maxZoom:15)` — 반경 1.0~1.3km 가장자리 호텔들이 bounds를 넓혀서 어느 지역을 골라도 비슷한 광역 뷰가 됨. (generate.py 검색 페이지 JS, fitBounds 호출부)

**수정** (drawMap 호출부 또는 내부):
```js
// run() 안에서 — 지역이 선택돼 있으면 fitBounds 대신 지역 중심으로 고정 줌
if (fArea) {
    var a = AREAS.filter(function(x){ return x.code === fArea; })[0];
    if (a) { syncMap = false; map.once('moveend', function(){ syncMap = true; renderVisible(); });
             map.setView([a.lat, a.lng], 15); }   // zoom 15 ≈ 반경 1.2km가 화면에 딱
    drawMap(pool, false);                          // fit 안 함
} else {
    drawMap(pool, true);                           // 전체일 때만 기존 fitBounds
}
```
- zoom 값: **15 고정** (모바일 360px 기준 반경 ~1.2km 시야 — AREAS r값과 일치). 나카스·기온(r=1.0)도 15로 통일 (지역별 줌 차등은 과설계).
- 메인 인기지역 → `?area=` 진입 시에도 같은 경로(run)를 타므로 자동 해결.
- 추천 결과 모드(§4)에서 지역이 설정된 경우도 동일 규칙.

---

## 2. 진입점: 플로팅 버튼 (메인)

- 위치: 메인(index)에만, 우하단 고정. **기존 #float 패턴 재사용** (상세의 공유/맨위로와 동일한 좌표계: `bottom 20px`, 스크롤 50px↑에서 표시... 단 이 버튼은 **처음부터 항상 표시**).
- 형태: 아이콘+라벨 **pill 버튼** (원형 아이콘 버튼과 달리 후킹용이므로 라벨 필수):
  ```html
  <section id="float"><div class="float">
      <a href="javascript:;" class="btn-airec">✨ <span>AI 추천받기</span></a>
  </div></section>
  ```
- 스타일: `background: linear-gradient(90deg, var(--primary) 0%, var(--primary-deep) 100%)` (실망확률 배지와 동일 그라데이션 = 브랜드 일관), 흰 글자 `--fs-body`/`--fw-bold`, 높이 48px, 라운드 48px, `--shadow-float`. 살짝 뜨는 느낌의 idle 애니메이션은 **넣지 않는다** (과한 모션 금지, prefers-reduced-motion 고려).
- 탭 → 추천 마법사 바텀시트 오픈 (CFNav.push 연동 — 뒤로가기로 시트만 닫힘).

---

## 3. 추천 마법사 — 바텀시트 3스텝

컨테이너: **기존 바텀시트 패턴 그대로** (`.cf-modal` + slide-up 카드, 그랩바, 우상단 ✕). 스텝 전환은 시트 내부 내용 교체 + 상단 진행 점 3개(●○○). 각 스텝은 질문 1개 + 칩 선택 + 하단 버튼 1개 — **한 화면에 하나의 결정만**.

### Step 1 · 우선순위 (핵심 스텝)

> **"여행할 때 이것만은 못 참아요 😤"**
> 중요한 순서대로 최대 3개 골라주세요

6개 칩 (2열 그리드, 기존 `.cft-chip` 패턴). **고객 언어 라벨 — 아래 표 그대로, 임의 수정 금지** (내부 용어 '위생 경보' 등은 절대 노출하지 않는다):

| 내부 카테고리 | 칩 라벨 | 보조 키워드(칩 아래 작게) |
|---|---|---|
| 위생 경보 | 🧼 더러운 건 못 참아 | 침구·벌레·곰팡이 |
| 오감 지옥 | 👂 시끄럽고 냄새나면 싫어 | 소음·악취 |
| 시설 사기단 | 🏚️ 낡은 시설은 실망이야 | 노후·냉난방·와이파이 |
| 동선 파괴자 | 🗺️ 위치가 제일 중요해 | 역까지 거리·접근성 |
| 불친절 레이더 | 💬 불친절은 못 넘어가 | 직원 응대 |
| 안전 그림자 | 🔒 안전이 최우선이야 | 보안·치안 |

- 탭 순서대로 칩 좌상단에 **①②③ 순위 뱃지** (보라 원+흰 숫자). 다시 탭하면 해제·순위 재정렬. 4번째 탭은 무시(3개 초과 금지, 짧은 흔들림 모션 대신 토스트 "3개까지 고를 수 있어요").
- 최소 1개 선택해야 [다음] 활성화.

### Step 2 · 예산

> **"1박 예산은 어느 정도예요? 💸"**

칩 4개 (1열 세로 나열, 큼직하게): `상관없어요` / `10만원 미만` / `10~20만원` / `20만원 이상` (기존 PRICE_BANDS 재사용). 단일 선택, 선택 즉시 다음 스텝 자동 진행(탭 수 절약).

### Step 3 · 지역

> **"어느 동네에 머물까요? 📍"**

칩 5개: `어디든 좋아요` / `하카타역` / `텐진` / `나카스·캐널시티` / `기온·오호리` (기존 AREAS 재사용). 각 칩 아래 한 줄 설명: 하카타역="신칸센·공항 이동 편리", 텐진="쇼핑·맛집 중심가", 나카스="야타이·나이트라이프", 기온="조용한 구시가". 선택 즉시 → **결과로 이동**.

버튼 카피: Step1 [다음 →], Step3 선택 시 자동 이동이므로 별도 완료 버튼 없음. 시트 하단에 항상 작게 "3초면 끝나요" 류의 재촉 문구는 **넣지 않는다** (조용한 신뢰 톤 유지).

**v2 백로그** (이번에 구현하지 않음): Step 0 "누구랑 가세요?"(혼자/커플/가족/친구) → 가족이면 안전·청결 가중 프리셋. 스텝 추가는 마찰이므로 검증 후 결정.

---

## 4. 결과 화면 — search.html 재사용 (신규 페이지 없음)

마법사 완료 → `search.html?rec=1&pr=hyg,sen,fac&bud=b1&area=hakata` 로 이동.
(pr = 우선순위 카테고리 코드 순서대로. 코드 맵: hyg=위생 경보, sen=오감 지옥, fac=시설 사기단, loc=동선 파괴자, svc=불친절 레이더, saf=안전 그림자)

URL로 상태를 전달하므로 **결과를 그대로 공유 가능** (부수 이점 — 공유 버튼에 활용 가능, v2).

### rec 모드에서 search.html의 변화

1. **헤더 블록** (filters 위에 삽입, 기존 notice-card 스타일 재사용):
   - "✨ 내 조건 맞춤 추천" (fs-h3/800)
   - 조건 요약 칩 줄: `① 🧼 청결` `② 👂 조용` `③ 🏚️ 시설` `💸 10만원 미만` `📍 하카타역` (읽기 전용, .sheet-chip 모양)
   - [조건 다시 설정] 텍스트 버튼 → 마법사 시트 재오픈(현재 값 프리셋)
2. **기존 필터 바는 숨김** (rec 조건과 이중 조작 혼란 방지). 정렬 드롭다운도 숨김 — 추천 순위가 곧 정렬.
3. **리스트 = 맞춤 점수 TOP 10** (§5):
   - 1~3위: 카드 좌상단 `🥇🥈🥉` 메달 + **추천 이유 한 줄** (아래 규칙)
   - 4~10위: 순위 숫자(4.~10.) + 이유 한 줄
   - 이유 문장 생성 규칙 (템플릿, 선택한 우선순위 기준):
     - 1순위 카테고리가 양호(<45)면: "선택하신 **{라벨}** 걱정이 적은 곳이에요 (후쿠오카 상위 {pctl}%)"
     - 실망확률이 도시평균 이하면 덧붙임: "· 실망 확률 {p}%"
     - 예산 설정 시: "· 1박 {pt}"
     - ⚠️ 매칭 퍼센트("92% 일치") 같은 **만들어낸 숫자는 쓰지 않는다** — 근거 있는 실측치(위험도·백분위·실망확률·가격)만 문장에 사용 (UI-STANDARDS "거짓 없는 숫자" 원칙).
4. **지도 = 순위 마커**: TOP 10만 표시. 기존 circleMarker 대신 `L.divIcon`으로 **보라 원 + 흰 순위 숫자** (1~3위 지름 32px, 4~10위 26px, 배경 var(--primary), 테두리 흰 2px). 팝업은 기존 팝업 + 첫 줄에 "🥇 1위". 지역 설정 시 §1 규칙(setView 15), 미설정 시 TOP10에 fitBounds.
5. **빈 결과 처리**: 조건 통과 호텔 < 3개면 리스트 위에 안내 카드 — "조건에 딱 맞는 곳이 {n}곳뿐이에요. 예산을 넓히면 더 보여드릴 수 있어요" + [예산 넓혀 다시 보기](bud 제거 후 재계산) / [지역 넓혀 다시 보기](area 제거). 자동 완화는 하지 않는다(고객 통제감 유지).

---

## 5. 맞춤 점수 산식 (matchScore, 클라이언트 JS)

후보 = `scored`(점수 노출) 호텔 중 **예산·지역 하드 필터** 통과한 곳. (예산 '상관없어요'면 전체·가격없는 호텔 포함, 예산 지정 시 `pb===bud`만. 지역은 AREAS 반경 필터 — 기존 passFilters의 km() 재사용.)

```
안심도(cat) = 100 − cs[cat]                    // cs = 카테고리 위험도(0~100, 평균 50)
우선순위항 = Σ 안심도(pr[i]) × w[i]             // w = [0.5, 0.3, 0.2] (1개 선택 시 [1.0], 2개 시 [0.6, 0.4])
전체안심항 = 100 − min(p, 40) / 40 × 100        // p = 실망확률%. 40%를 바닥으로 정규화
구글신뢰항 = norm( (g×rc + 4.0×100) / (rc+100) ) // 베이지안 평점(사전평균 4.0, 가중 100건).
                                                  // norm: 3.5→0점, 4.6→100점 선형, clamp
matchScore = 우선순위항 × 0.60 + 전체안심항 × 0.25 + 구글신뢰항 × 0.15
동점이면 실망확률 낮은 순 → 리뷰 많은 순
```

- 구글 평점·리뷰수는 사용자 요청대로 **수치로 반영**하되 베이지안 보정으로 리뷰 적은 호텔의 과대 평점을 눌러준다 (리뷰수가 자연스럽게 신뢰 가중치가 됨).
- matchScore는 **정렬용 내부 값** — 화면에 점수로 노출하지 않는다 (이유 문장으로만 설명).
- 참고 등급(30~199건) 호텔이 후보에 섞이는 문제: v1에서는 scored 전체 허용하되, 동일 점수대에서 분석 리뷰 수가 많은 쪽이 앞서도록 동점 규칙이 커버. (§7.0 모수 정책과 일관되게 analyzed 수 = 텍스트+별점 모수.)

---

## 6. 디자인 가드레일 (Opus 필수 준수 — 어기면 일관성 붕괴)

1. **색**: tokens.css 변수만. 순위 마커·뱃지·CTA 전부 `--primary`/`--primary-deep`. 메달은 이모지(🥇🥈🥉)로 해결 — 금/은/동 커스텀 색 만들지 말 것.
2. **폰트**: 8단계 스케일(`--fs-*`) 외 px 금지. 시트 제목 = `--fs-h3`+`--fw-display` (기존 sheet-title과 동일).
3. **컴포넌트 재사용 맵** (새로 만들지 말고 이것을 복제):
   | 필요한 것 | 재사용할 기존 패턴 |
   |---|---|
   | 마법사 바텀시트 | `.cf-modal` + `.cf-modal-card` (종료확인/피드백 모달과 동일 구조) |
   | 선택 칩 (Step1) | `#cf-catfilter .cft-chip` (2열 그리드 포함) |
   | 예산/지역 칩 | `.cft-chip` 1열 변형 |
   | 하단 버튼 | `.cf-btn.cf-btn-primary` (52px) |
   | 조건 요약 칩 | `.sheet-chip` |
   | 결과 안내 카드 | `#search .notice-card` |
   | 진행 점 3개 | 신규 최소 CSS (8px 원, `--primary`/`--line-strong`) — 유일하게 새로 만드는 것 |
4. **간격·라운드**: `--sp-*` 4px 그리드, 라운드 20/16/14/8 4단계만.
5. **카피 톤**: 이 문서의 문구 그대로. "~해요"체, 이모지는 문구당 1개, 느낌표 남발 금지.
6. **flex column 버튼 함정**: `.cf-modal-btns`가 column일 때 `.cf-btn`에 `flex:0 0 auto` 필수 (이번에 겪은 높이붕괴 버그 — mvp.css에 이미 주석 있음).
7. 완료 후 **UI-STANDARDS.md에 "AI 추천" 섹션 추가**하고 라벨 표를 옮겨 적을 것 (표준문서가 정본이 되도록).

---

## 7. 구현 체크리스트 (Opus 작업 순서)

1. [ ] §1 지도 줌 수정 (generate.py 검색 JS run() — 5줄) → 재생성 → 지역 칩 4개 각각 눌러 zoom 15 확인
2. [ ] 마법사 시트 마크업+JS (index에 삽입, generate.py build_index) — CFNav.push 연동, 뒤로가기로 스텝 후퇴가 아닌 시트 닫힘(v1 단순화)
3. [ ] 플로팅 버튼 (index #float) + mvp.css `.btn-airec`
4. [ ] search.html rec 모드: URL 파싱 → 헤더 블록 렌더 + 필터/정렬 숨김 + matchScore 정렬 TOP10 + 이유 문장 + 순위 divIcon 마커
5. [ ] 빈 결과(<3) 완화 제안 버튼 2개
6. [ ] [조건 다시 설정] → 마법사 재오픈(값 프리셋) — 마법사 코드를 index/search 공용 JS(engage.js 또는 신규 recommend.js)로 분리
7. [ ] UI-STANDARDS.md에 AI 추천 섹션 추가
8. [ ] QA: ① 우선순위 1개만/3개 선택 각각 결과 다른지 ② 예산 하드필터 동작 ③ 지역 줌 15 ④ 뒤로가기(시트 닫힘/결과→메인) ⑤ 공유 URL 재진입 시 동일 결과 ⑥ 참고: 상관없어요+어디든 좋아요 = 전체 142곳에서 TOP10
9. [ ] 커밋 → 배포 (Pages 실패 시 빈 커밋 재푸시)

---

## 8. 카피 최종본 (그대로 사용)

- 플로팅: `✨ AI 추천받기`
- 시트 타이틀: `나에게 딱 맞는 호텔 찾기`
- Step1: `여행할 때 이것만은 못 참아요 😤` / 부제 `중요한 순서대로 최대 3개 골라주세요`
- Step2: `1박 예산은 어느 정도예요? 💸`
- Step3: `어느 동네에 머물까요? 📍`
- 결과 헤더: `✨ 내 조건 맞춤 추천` / 부제 `선택하신 조건으로 {n}곳 중에서 골랐어요`
- 빈 결과: `조건에 딱 맞는 곳이 {n}곳뿐이에요` / `예산을 넓히면 더 보여드릴 수 있어요`
